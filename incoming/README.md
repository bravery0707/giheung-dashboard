# 원본 파일은 이 폴더에 업로드하지 않습니다

엑셀·매물 JSON은 개인 PC에서 `node scripts/import-listings.mjs "파일 경로" --date YYYY-MM-DD`로 가져옵니다. 조사 이력은 `.private/listing-data/`에 저장됩니다.

이 폴더의 원본 업로드/자동 반영은 중단했습니다. 공개 저장소에 한 번 올린 파일은 삭제 커밋을 만들어도 과거 Git 이력에 남습니다.
